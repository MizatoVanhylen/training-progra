/*
hacer un array, y que diga cuantos elementos hay dentro del array en - 
y ese menos se le resta a 10

tambien el numeros.lenth se puede multiplicar por -1
*/

const numeros = [4, 6, 9, 14, 30, 7];

const total = -numeros.length;

console.log(total);

const operación = (10 + total);

console.log(operación);