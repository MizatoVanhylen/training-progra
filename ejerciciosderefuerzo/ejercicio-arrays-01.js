/**
 * Un Array es una constante que puede alvergar muchos valores
 */

const pokemon = ["Eevee", "Flareon", "Vaporeon", "Jolteon"];
const pokemon2 = ["Eevee", "Flareon", "Vaporeon"];
const juego = pokemon2[pokemon2.length -1]
console.log(juego[juego.length -1]);

//obtener 

//console.log(pokemon[0]);
//console.log(4 - 1)

/*console.log(pokemon);

const numeros = [1 , 2 , 3 , 4];

console.log(numeros.filter(n => n > 2 ));

console.log(pokemon.sort())

pokemon.push("Umbreon")
console.log(pokemon);
console.log(pokemon.length);



/* mostrar ultimo valor de cualquier array creando uno nuevo con dos valores,
y que el console,log funcione para ambos
*/

/*
ejercicio

Crear un array donde se guarden numeros, se deben sumar el primer numero con el ultimo numero.
*/

/*
Crear un array de 4 numeros donse se sume el primer elemento con el segundo y el resultado se debe 
multiplicar con el tercer elemento y el resultado se divide por el ultimo elemento
*/
 
/*
Crear un array que el largo del array sea par, se debe cortar el array
de la mitad para atras y de la mitad para adelante
se debe revisar que hace .slice
*/ 

/**
 * Crear un array con pokemones  y agregar un pokemon nuevo  al array y realizar if que debe hacer que haga que en el caso
 * de que el nombre del pokemon tenga más de 8 caracteres lo agregue y si tienes menos de ocho caracteres agrege un ditto 
 * .push
 */
