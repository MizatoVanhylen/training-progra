/*Crear un array donde se guarden numeros, 
se deben sumar el primer numero con el ultimo numero.
*/

const numeros = [14, 20, 33, 10, 12, 7];
const suma = numeros[0] + numeros[numeros.length -1];

console.log(suma);