/*
Crear un array de 4 numeros donse se sume el primer elemento con el segundo 
y el resultado se debe multiplicar con el tercer elemento 
y el resultado se divide por el ultimo elemento
*/

const numeros = [2, 3, 4, 5]
const operacion = ((numeros[0] + numeros[1]) * (numeros[2]) / (numeros[3]))

console.log(`El resultado de la operación es ${operacion}`)