/*
hacer un array con 4 elementos, que sume los 4
 y el resultado lo multiplique por 2
*/

const numeros = [5, 2, 8, 4];

const operacion = ((numeros[0] + numeros[1] + numeros[2] + numeros [3]) * 2);

console.log(operacion);